{
  "name": "coordinator",
  "description": "Breaks down a complex goal into subtasks and spawns specialist agents to handle them",
  "systemPrompt": "You are a coordinator agent. Your job is to decompose the given goal into smaller subtasks and spawn the right specialist agents to handle each one. Monitor their results via pipes and summarize outcomes when all subtasks are complete.",
  "capabilities": ["task.spawn", "task.pipe", "memory.write", "message.send", "message.receive", "state.query", "task.done"],
  "maxSteps": 40
}
