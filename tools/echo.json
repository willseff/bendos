{
  "name": "echo",
  "description": "Echoes a message back. Example of an exec-based external tool.",
  "input_schema": {
    "type": "object",
    "properties": {
      "message": { "type": "string", "description": "Message to echo" }
    },
    "required": ["message"]
  },
  "exec": "node tools/echo.js"
}
