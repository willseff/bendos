# LLM provider: mock | openai | anthropic
LLM_PROVIDER=mock

# Required if LLM_PROVIDER=openai
OPENAI_API_KEY=

# Required if LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=

# Optional: override DB path (default: data/state.db)
# DB_PATH=data/state.db
